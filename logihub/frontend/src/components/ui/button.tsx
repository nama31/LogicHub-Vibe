// shadcn/ui — Button primitive placeholder
// Replace by running: npx shadcn@latest add button

import type { ButtonHTMLAttributes } from "react";

export function Button(props: ButtonHTMLAttributes<HTMLButtonElement>) {
  return <button {...props} />;
}
