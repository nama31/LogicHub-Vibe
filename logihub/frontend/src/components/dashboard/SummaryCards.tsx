"use client";

// Fetches GET /analytics/summary
// Displays: today.orders_created, today.net_profit_som,
//           this_week.net_profit_som, open_orders counts, stock_alerts

export function SummaryCards() {
  return (
    <section aria-label="Сводка">
      {/* TODO: render summary cards from /analytics/summary */}
    </section>
  );
}
